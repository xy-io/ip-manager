// IPListView.swift
// This file is intentionally minimal.
// The list is rendered by SidebarView inside RootView.
// Kept for Xcode project compatibility.
import SwiftUI

struct IPListView: View {
    var body: some View { EmptyView() }
}
